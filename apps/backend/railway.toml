[build]
builder = "RAILPACK"
buildCommand = "pnpm install && pnpm --filter backend prisma:generate && pnpm --filter backend... build"

[deploy]
startCommand = "cd apps/backend && pnpm run prisma:migrate && pnpm run start"
